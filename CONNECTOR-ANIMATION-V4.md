# Connector Animation V4

- Replaced fixed SVG coordinates with runtime-measured connectors.
- Every connector now begins at a defined stack anchor and ends one pixel inside the exact vertical center of its matching card.
- Added continuous dotted-line motion, a staggered traveling light pulse, and synchronized endpoint breathing.
- Connectors redraw automatically when the component, image, or cards resize.
- Mobile hides the illustration and connectors cleanly; reduced-motion users receive a static version.
