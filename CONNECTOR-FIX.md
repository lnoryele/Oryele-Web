# Animated connector correction

- Removed the six detached horizontal dotted rules.
- Added one responsive SVG connector map spanning the platform stack and capability cards.
- Each connector starts at a corresponding stack layer, follows a clean orthogonal path, and terminates at the vertical center of its matching card.
- Added continuous directional dash animation with staggered timing.
- Added reduced-motion support.
- Connectors are hidden on mobile when the illustration is removed.
